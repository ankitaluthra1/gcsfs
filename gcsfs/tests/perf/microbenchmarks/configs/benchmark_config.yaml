# Common configuration applied to all benchmark runs.
common:
  project: "gcs-aiml-clients-testing-101"
  profile: "no" # "yes" or "no"
  buckets:
    regional: "jasha-test-bucket-gcsfs-usc1"
    hns: ""
    zonal: ""

# A list of benchmark scenarios to run.
# Each scenario will be run against all buckets defined in the common section.
benchmarks:
  # Read benchmarks

  - name: "read_1gb_file_100mb_chunk_seq"
    group: "read" 
    params:
      num_files: 1
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 1
      pattern: "seq"

  - name: "read_1gb_file_100mb_chunk_seq_16_threads"
    group: "read" 
    params:
      num_files: 1
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 16
      pattern: "seq"

  - name: "read_1gb_file_100mb_chunk_seq_32_threads"
    group: "read" 
    params:
      num_files: 1
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 32
      pattern: "seq"

  - name: "read_1gb_file_100mb_chunk_seq_16_threads_16_files"
    group: "read" 
    params:
      num_files: 16
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 16
      pattern: "seq"

  - name: "read_1gb_file_100mb_chunk_seq_32_threads_32_files"
    group: "read" 
    params:
      num_files: 32
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 32
      pattern: "seq"

  - name: "read_1gb_file_100mb_chunk_rand"
    group: "read"  
    params:
      num_files: 1
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 1
      pattern: "rand"

  - name: "read_1gb_file_100mb_chunk_rand_16_threads"
    group: "read"  
    params:
      num_files: 1
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 16
      pattern: "rand"

  - name: "read_1gb_file_100mb_chunk_rand_32_threads"
    group: "read"  
    params:
      num_files: 1
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 32
      pattern: "rand"

  - name: "read_1gb_file_100mb_chunk_rand_16_threads_16_files"
    group: "read"  
    params:
      num_files: 16
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 16
      pattern: "rand"

  - name: "read_1gb_file_100mb_chunk_rand_32_threads_32_files"
    group: "read"  
    params:
      num_files: 32
      file_size: 1024 # in MB
      chunk_size: 100 # in MB
      rounds: 10
      threads: 32
      pattern: "rand"
